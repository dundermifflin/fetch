//should query database and pull dog that matches user selection for activity
	//should return FIRST item that matches the designated activity
	//should set availability to false once dog has been pulled
	//should return the dog object 